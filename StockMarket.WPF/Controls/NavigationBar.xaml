<UserControl x:Class="StockMarket.WPF.Controls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockMarket.WPF.Controls"
             xmlns:nav="clr-namespace:StockMarket.WPF.States.Navigators"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Stock Market App" Background="{StaticResource BrushBlack}" Foreground="{StaticResource BrushYellow1}" Padding="10,10,10,10" FontWeight="Bold" FontSize="16" />
        <Separator Grid.Row="0" VerticalAlignment="Bottom" Background="{StaticResource BrushBlackFull}" Margin="0"/>

        <Grid Grid.Row="1" Background="{StaticResource BrushBlack}">
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <RadioButton Grid.Column="0" Content="Home" Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static nav:ViewType.Home}" Foreground="{StaticResource BrushYellow1}" Padding="7" VerticalContentAlignment="Center"  BorderThickness="0"/>
            <RadioButton Grid.Column="1" Content="Portfolio" Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static nav:ViewType.Portfolio}" Foreground="{StaticResource BrushYellow1}" Padding="7" VerticalContentAlignment="Center"  BorderThickness="0"/>
            <RadioButton Grid.Column="2" Content="Buy" Foreground="{StaticResource BrushYellow1}" Padding="7" VerticalContentAlignment="Center"  BorderThickness="0" />
            <RadioButton Grid.Column="3" Content="Sell" Foreground="{StaticResource BrushYellow1}" Padding="7" VerticalContentAlignment="Center" BorderThickness="0" />
        </Grid>
        <Separator Grid.Row="1" VerticalAlignment="Bottom" Background="{StaticResource BrushBlackFull}" Margin="0" MinHeight="5" />
    </Grid>
    
</UserControl>
